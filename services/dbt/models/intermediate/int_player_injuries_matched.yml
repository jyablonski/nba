version: 2

models:
  - name: int_player_injuries_matched
    description: Injuries after provider-key resolution at ingest.
    columns:
      - name: player_id
        data_tests:
          - not_null
      - name: team_id
        data_tests:
          - not_null
      - name: match_method
        data_tests:
          - dbt_expectations.expect_column_values_to_be_in_set:
              arguments:
                value_set: ["external_id"]
                quote_values: true
